interface:
  display_name: "onepage-resume"
  short_description: "Generate a one-page resume from user content with style selection, section confirmation, and HTML/PDF export."
  default_prompt: "Use onepage-resume for this task."
policy:
  allow_implicit_invocation: true
